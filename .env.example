# Data Dispatcher Service Environment Configuration
# Cloud-Native Kubernetes Deployment

# RabbitMQ Configuration
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASS=guest

# PostgreSQL Configuration (Cloud SQL)
POSTGRES_HOST=127.0.0.1  # localhost when using Cloud SQL Proxy sidecar
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASS=your_password_here
POSTGRES_DB=mep_database

# Worker Pool Configuration
WORKER_POOL_SIZE=10

# Kubernetes Pod Name (injected via Downward API)
# This is automatically set by Kubernetes - do not set manually
# POD_NAME=data-dispatcher-xxxxx-yyyyy

# Optional: Logging Level (debug, info, warn, error)
LOG_LEVEL=info
